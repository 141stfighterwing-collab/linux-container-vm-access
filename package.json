{
  "name": "linux-container-vm-access",
  "version": "0.1.0",
  "private": true,
  "description": "Linux-only lightweight browser-accessible container desktop sessions with VNC/RDP/SSH endpoints.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "build:image": "docker build -t lcva/desktop:latest images/desktop",
    "lint": "node --check src/server.js && node --check src/docker.js && node --check scripts/smoke.js",
    "smoke": "node scripts/smoke.js"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
